(
	let(
		(one, 1)
		(
			plusOne,
			letType(
				object(
					(
						cl.Closure,
						initialiser: (takes: (term, s32) (one, s32), in: record((term, term) (one, one))),
						methods: (
							invoke,
							takes: (term, s32),
							returns: s32,
							in: let(
								(term, field(term, of: self)) (one, field(one, of: self)),
								in: value(binary(one, add, term))
							)
						)
					)
				),
				in: message(object(cl.Closure, ), invoke)
			)
		),
		in: evaluate(plusOne, 2)
	)
)