(
	let(
		(one, 1)
		(
			plusOne,
			letType(
				object(
					(
						cl.Closure,
						constructor: (takes: (one, s32) (term, s32), in: record((one, one) (term, term))),
						methods: (
							invoke,
							takes: (term, s32),
							returns: s32,
							in: let(
								(one, field(one, of: self)) (term, field(term, of: self)),
								in: value(binary(one, add, term))
							)
						)
					)
				),
				in: message(object(cl.Closure, ), invoke)
			)
		),
		in: evaluate(plusOne, 2)
	)
)