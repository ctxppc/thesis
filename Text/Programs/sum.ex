(
	evaluate(function(l.anon), constant(1080) constant(-80)),
	functions: (
		l.anon,
		takes: (first, s32(), sealed: false) (second, s32(), sealed: false),
		returns: s32(),
		in: value(binary(named(first), add, named(second)))
	)
)