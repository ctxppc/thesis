(
	do(
		set(
			df.result,
			to: do(
				set(ls.answer, to: source(constant(42))),
				then: do(
					set(ls.answer$1, to: source(constant(28))),
					then: source(location(ls.answer))
				)
			)
		)
		return(location(df.result))
	),
	procedures: 
)